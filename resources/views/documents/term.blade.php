@extends('documents.base') @section('content')
<div class="head"><div class="brand">@if(!empty($snapshot['company']['logo']))<img src="{{ storage_path('app/private/'.$snapshot['company']['logo']) }}">@else<h1>ARL</h1>@endif</div><div class="company"><h1>{{ $snapshot['company']['company_name'] ?? $snapshot['company']['name'] ?? 'ARL Informática' }}</h1><div>{{ $snapshot['company']['phone'] ?? '' }} · {{ $snapshot['company']['email'] ?? '' }}</div></div></div>
<h2>TERMO DE RECEBIMENTO E RETIRADA DE EQUIPAMENTO</h2><div class="box"><b>OS nº {{ $order['number'] }}</b> · Entrada: {{ date('d/m/Y H:i', strtotime($order['received_at'])) }}<br><b>Cliente:</b> {{ $snapshot['client']['name'] }} · {{ $snapshot['client']['document'] }}<br><b>Problema relatado:</b> {{ $order['reported_problem'] }}<br><b>Atendimento:</b> {{ $order['attendance_type']==='bench'?'Análise na Bancada':'Atendimento Externo' }}</div>
<div class="box"><b>ESTADO FÍSICO NA ENTRADA:</b> {{ data_get($order, 'intake_condition') ?: 'Equipamento aparentemente 100% sem avarias' }}</div><div>{!! nl2br(e($snapshot['term_text'])) !!}</div><div class="footer">{{ $snapshot['company']['instagram'] ?? '' }}</div>@endsection
